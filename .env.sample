# Financial Transaction Visualizer - Environment Variables
# Copy this file to .env and configure the values

# Base URL for the IFSC lookup API (defaults to https://ifsc.razorpay.com/)
RAZORPAY_IFSC_API_URL=https://ifsc.razorpay.com/

# Optional API Key for custom proxy or API endpoint if required
IFSC_API_KEY=
