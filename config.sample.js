// Configuration File
// Copy this file to config.js and populate the values
window.APP_CONFIG = {
    // The base URL for the Razorpay IFSC API.
    // This allows you to route requests through a proxy or use a different endpoint if required.
    RAZORPAY_IFSC_API_URL: "https://ifsc.razorpay.com/"
};
